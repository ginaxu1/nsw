apiVersion: v1
kind: Service
metadata:
  name: {{ include "oga-backend.fullname" . }}
  labels:
    {{- include "oga-backend.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "oga-backend.selectorLabels" . | nindent 4 }}
